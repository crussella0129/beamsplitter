import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Beamsplitter — Mirror Posts, Compare Engagement",
  description:
    "Post simultaneously to X and Threads, then compare engagement performance side-by-side. One post, two platforms, clear insights.",
  keywords: ["social media", "cross-posting", "X", "Threads", "analytics", "engagement"],
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
